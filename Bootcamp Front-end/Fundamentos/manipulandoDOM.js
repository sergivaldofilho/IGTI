// Manipulando o DOM
// Representa uma árvore de objetos em memória
// No caso da webkitCancelAnimationFrame, mapeia documentos HTML em objetos
// Principais comandos são querySelector() e querySelectorAll()

// Comando querySelector
//Utilizado com a comando 'document' - document.querySelector
// Pode ser utilzado em elementos, classes e id's HTML

// Comando querySelectorAll
// Retorna documentos do tipo NodeList. Para converter para array (mais comum),
// usa-se o comando Array.from()
// Pode ser utilzado em elementos, classes e id's HTML

// A propriedade textContent
// Refere-se ao conteudo textual de alguns elementos

// Manipulando CSS com JavaScript
// Não é interessante utilizar comandos css no JavaScript.
// O ideal é definiar as classes em arquivos isolados e utilizar
// as ferramentas classList.add() ou classList.remove()
